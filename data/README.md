# Dataset

This folder contains a curated sample of the project dataset:

- `labels.txt`: label definitions.
- `sample_images/`: sample images used for visual object detection.
- `annotations/`: XML annotations corresponding to the sample images.

The dataset is included to show the model training and annotation workflow. For public GitHub presentation, this repository keeps the dataset compact and reviewable.
